# Architect Validation Report — validate run 2026-09-25-463eecdd

**Scope:** `all` (architecture lane). **Subject:** `docs/utopia-evolution` @ `d897b46` against `main` @ `93cc8c1`. **Rerun of:** `2026-09-25-9aa87cdf`.
**Role:** Architect. **Framework:** `nebula-agents` at the pinned commit `c218bf1`.

## Ontology Integrity

| Check | Result | Evidence |
|---|---|---|
| Integrity, drift, symbols | PASS (0 errors; pre-existing unused-edge-type warnings only) | `artifacts/test-results/kg-check-drift.txt`, `kg-check-symbols.txt` |
| Reproducibility | PASS | `kg-check-reproducible.txt` |
| Orphans | 27 (unchanged from the prior run; 30 on `main`) | `kg-check-orphans.txt` |
| Dead code (`--safe-only`) | Unchanged pre-existing F0001 candidates; no code changed | `kg-dead-code-safe-only.txt` |

The fix commit edits only three ADR files and the regenerated `coverage-report.yaml`; no `kg-source` shard changed. The coverage report was regenerated by `validate.py --write-coverage-report` (not hand-edited), and reproducibility confirms it.

## Canonical Nodes

Unchanged since the prior run. The seven ADR nodes resolve; no new node kinds.

## Feature Mappings

Unchanged since the prior run: 4 mapped, 62 excluded, 0 uncovered. F0066 is reserved at v0.2B.

## API Contracts

Unchanged; `api/brain-api.yaml` is untouched.

## Schemas

Unchanged. Finding A-5 is carried to feature planning.

## Authorization

- **A-1 closed.** ADR-0067 §1 now stores the full impact detail under the underlying resources' authorization. The detail a reviewer sees is re-evaluated against that reviewer's current authorization at display time. When any contributing impact is hidden from the reviewer, only a generic non-revealing `IMPACT_HOLD` is shown. A reviewer without authority over every affected resource cannot approve. Holds are computed identically whatever the viewer. A proof-gate case was added. This is consistent with master blueprint §107.4, ADR-0042, ADR-0044, ADR-0052, and ADR-0053.
- **A-2 closed.** ADR-0066 §1a makes name facts KB-owned semantic content, consistent with ADR-0061. The alias projection is per KB and authorization-filtered. Names are never displayed, searched, or used for resolution across KBs. A cross-KB negative proof case was added.

## Assembly-Plan Alignment

- **Unchanged:** F0002 is untouched.
- **Carried forward:** A-3 (F0065 contract and `basis_hash`) and A-4 (F0005 activation criteria), routed to feature owners at the prior run's V3.

## Findings

| ID | Severity | Status | Note |
|---|---|---|---|
| A-1 | Medium | **Closed** in `d897b46` | ADR-0067 §1 and proof gates |
| A-2 | Medium | **Closed** in `d897b46` | ADR-0066 §1a and proof gates |
| A-3 | Medium | Open, routed | F0065 plan update, or deferral to F0056 |
| A-4 | Medium | Open, routed | F0005 S0004 activation criteria, or deferral with rationale |
| A-5 | Low | Open, routed | Schema-version obligations at F0006/F0016/F0022 plan runs |
| A-6 | Low | Open, expected | New ADR orphans clear at each feature's `plan` G4 |
| A-7 | Low | Open, routed | §28 field list at the F0016 plan run |
| A-8, A-9 | Info | Open, routed | `nebula-agents` defects: init-run, manifest wording, exec-and-log capture, V3 attestation |

No Critical or High findings. No new findings.

## Result

**PASS WITH RECOMMENDATIONS.** The knowledge graph is intact and the security clarifications are closed. The remaining findings are routed to their owners and do not block integration.
