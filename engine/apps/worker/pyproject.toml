[project]
name = "brain-worker"
version = "0.1.0"
description = "Outbox projector and engine-owned durable document job dependencies."
requires-python = ">=3.13"
dependencies = [
  "sqlalchemy>=2.0.52",
  "asyncpg>=0.30",
  "brain-temporal",
  "brain-persistence",
  "brain-jobs",
  "brain-content",
  "brain-security",
  "brain-review",
  "brain-contracts",
]

[tool.uv.sources]
brain-temporal = { workspace = true }
brain-persistence = { workspace = true }
brain-jobs = { workspace = true }
brain-content = { workspace = true }
brain-security = { workspace = true }
brain-review = { workspace = true }
brain-contracts = { workspace = true }

[project.optional-dependencies]
test = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "pytest-asyncio>=0.24",
  "aiosqlite>=0.20",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
brain_worker = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--import-mode=importlib"
